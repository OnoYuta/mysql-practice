sync:
  app:
    mode: "two-way-resolved"
    alpha: "./"
    beta: "mysql-practice:/home/vagrant/app"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"